# No environment variables are required for the current MVP.
# Supabase is intentionally not connected yet because these calculators
# work entirely in the browser. Add variables here later if we add accounts,
# saved calculations, admin tools, analytics, or other backend features.
